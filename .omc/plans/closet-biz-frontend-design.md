# ClosetBiz - 프론트엔드 구현 설계서

> **문서 버전**: v1.0
> **작성일**: 2026-02-14
> **참조 문서**: `closet-biz-system-final.md` (시스템 계획), `dark-mode-multiview-ux-design.md` (디자인 시스템)
> **이 문서의 범위**: 컴포넌트 설계, 페이지 레이아웃, 상태 관리, CSS 아키텍처, 접근성, 에러/로딩/빈 상태 패턴

---

## 1. 디자인 시스템 컴포넌트 계층

### 1.1 Atomic Design 구조

shadcn/ui를 Atoms 레이어로 직접 사용한다. shadcn/ui는 npm 패키지가 아닌 복사-소유 방식이므로 프로젝트에 맞게 자유롭게 커스터마이징한다. 별도 Atoms 래퍼를 만들지 않는다.

```
Atoms = components/ui/ (shadcn/ui 기반, 커스터마이징 가능)
├── 기본 UI: Button, IconButton, Input, Textarea, Badge, Avatar
├── 선택/토글: Checkbox, Radio, Toggle, Switch, Select
├── 피드백: Skeleton, Spinner, Progress, Tooltip
├── 표시: Label, Separator, Card
├── 오버레이: Dialog, Sheet, DropdownMenu, Popover
├── 내비게이션: Tabs, NavigationMenu, Command
└── 폼: Form (react-hook-form 통합)

Molecules = components/common/ (shadcn Atoms를 조합)
├── FormField (Label + Input + ErrorMessage)
├── SearchInput (Icon + Input + Clear)
├── StatusBadge (Badge 커스텀 + StatusDot)
├── OrderStatusChip (색상 코딩 + 텍스트 + 아이콘)
├── InventoryGauge (Progress + 수량 텍스트)
├── NumberSpinner (Input + IconButton[-/+], 장갑 대응)
├── DateDisplay (Icon + 날짜 + 상대시간)
├── PriceDisplay (금액 + 단위, font-mono)
├── EmptyState (Icon + 제목 + 설명 + CTA)
├── ErrorState (Icon + 메시지 + 재시도)
├── AlertBanner (Icon + 메시지 + 액션)
├── ConfirmDialog (Dialog 확장)
├── CustomerBrief (이름 + 전화 + 주소 한줄)
├── ThemeToggle / ThemeToggleCompact
├── ViewSwitcher (Tabs/Buttons/Dropdown)
└── QuickAddSheet (Sheet 확장, 모바일 바텀시트)

Organisms = components/{domain}/ (도메인별 기능 단위)
├── Layout (components/layout/)
│   ├── AppShell (Sidebar + Header + Content + BottomTabBar)
│   ├── Sidebar (Nav + ThemeToggle + User)
│   ├── Header (Title + Search + Actions)
│   ├── BottomTabBar (5 tabs + FAB)
│   └── PageHeader (Breadcrumb + Title + Actions + ViewSwitcher)
├── Data Display
│   ├── DataTable (정렬/필터/페이지네이션)
│   ├── KanbanBoard (칸반 컬럼 + 카드)
│   ├── TimelineView (시간축 + 노드)
│   ├── CalendarView (@schedule-x 래퍼)  # [M12] 한국어 로케일 PoC 필요
│   └── AgendaView (섹션 + 일정 카드)
├── Forms
│   ├── OrderForm (다단계 폼)
│   ├── CustomerForm
│   ├── ProductForm
│   ├── PurchaseOrderForm
│   └── ScheduleForm
├── Cards
│   ├── OrderCard (칸반/리스트용)
│   ├── InventoryCard (그리드용)
│   ├── ScheduleCard (어젠다용)
│   ├── DashboardStatCard (KPI)
│   └── FinanceCard (매출/매입)
├── Specialized
│   ├── OrderPipeline (칸반 보드)
│   ├── StatusTransitionButton
│   ├── OrderCancelDialog
│   ├── ShortageAlertDialog
│   ├── ScheduleConflictWarning
│   └── ClosetEditor (Three.js 에디터)
└── Charts
    ├── ChartWrapper (다크모드 어댑터)
    ├── RevenueSummaryChart
    ├── OrderStatusPieChart
    └── InventoryBarChart
```

### 1.2 재사용 컴포넌트 카탈로그

#### StatusBadge

모든 상태 표시에 사용하는 범용 배지.

```typescript
// components/ui/status-badge.tsx
interface StatusBadgeProps {
  status: string;
  colorMap: Record<string, string>;   // status -> tailwind color class
  labelMap: Record<string, string>;   // status -> 한국어 라벨
  size?: 'sm' | 'md';                 // sm: 12px, md: 14px
  showDot?: boolean;                  // 좌측 상태 점
}

// 사용 예시
<StatusBadge
  status={order.status}
  colorMap={ORDER_STATUS_COLORS}
  labelMap={ORDER_STATUS_LABELS}
/>
```

#### NumberSpinner

현장 장갑 착용 대응 숫자 입력.

```typescript
// components/ui/number-spinner.tsx
interface NumberSpinnerProps {
  value: number;
  onChange: (value: number) => void;
  min?: number;
  max?: number;
  step?: number;
  unit?: string;                     // "EA", "mm", "개"
  size?: 'default' | 'field';        // field: 56px 버튼
  disabled?: boolean;
}
```

#### EmptyState

빈 상태 안내.

```typescript
// components/ui/empty-state.tsx
interface EmptyStateProps {
  icon: LucideIcon;
  title: string;
  description: string;
  action?: {
    label: string;
    onClick: () => void;
  };
}

// 사용 예시
<EmptyState
  icon={Package}
  title="등록된 자재가 없습니다"
  description="자재를 등록하면 재고 관리를 시작할 수 있습니다."
  action={{ label: "자재 등록", onClick: () => router.push('/inventory/new') }}
/>
```

#### QuickAddSheet

모바일 바텀시트 기반 빠른 추가.

```typescript
// components/ui/quick-add-sheet.tsx
interface QuickAddSheetProps {
  trigger: ReactNode;                 // FAB 또는 버튼
  title: string;
  children: ReactNode;               // 폼 콘텐츠
  onSubmit: () => Promise<void>;
  submitLabel?: string;
}
// Radix Dialog + 모바일에서는 하단 슬라이드업
// 데스크탑에서는 centered dialog
```

---

## 2. 레이아웃 시스템

### 2.1 반응형 브레이크포인트

```
Mobile:   < 768px   (sm, default)  - 1열, 하단 탭 바
Tablet:   768-1023px (md)          - 2열, 접이식 사이드바
Desktop:  >= 1024px  (lg, xl, 2xl) - 사이드바 고정, 최대 4열
```

### 2.2 그리드 시스템

```css
/* 페이지 콘텐츠 최대 너비 */
.page-content {
  @apply mx-auto max-w-7xl px-4 sm:px-6 lg:px-8;
}

/* 대시보드 그리드 */
.dashboard-grid {
  @apply grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4 lg:gap-6;
}

/* 카드 그리드 (재고 등) */
.card-grid {
  @apply grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4;
}

/* 2열 폼 (데스크탑) */
.form-grid {
  @apply grid grid-cols-1 lg:grid-cols-2 gap-x-6 gap-y-4;
}
```

### 2.3 네비게이션 구조

#### 데스크탑: 좌측 사이드바 (240px, 접힘 시 64px)

```
┌──────────────────────────────────────────────────────────┐
│ ┌──────────┐ ┌──────────────────────────────────────────┐│
│ │          │ │  Header (Title + Search + ThemeToggle)   ││
│ │ CLOSET   │ ├──────────────────────────────────────────┤│
│ │ BIZ      │ │                                          ││
│ │          │ │                                          ││
│ │ ───────  │ │              Page Content                ││
│ │ 대시보드  │ │                                          ││
│ │ 수주관리  │ │                                          ││
│ │ 스케줄   │ │                                          ││
│ │ 재고관리  │ │                                          ││
│ │ 발주관리  │ │                                          ││
│ │ 매출/매입 │ │                                          ││
│ │ 리포트   │ │                                          ││
│ │ 모델링   │ │                                          ││
│ │          │ │                                          ││
│ │ ───────  │ │                                          ││
│ │ ☀/🌙    │ │                                          ││
│ │ 설정     │ │                                          ││
│ └──────────┘ └──────────────────────────────────────────┘│
└──────────────────────────────────────────────────────────┘
```

#### 모바일: 상단 헤더 + 하단 탭 바

```
┌─────────────────────────┐
│ ≡  페이지 제목     🔍 @ │  ← 상단 헤더 (햄버거 + 타이틀 + 검색 + 프로필)
├─────────────────────────┤
│                         │
│    Page Content         │
│    (full width)         │
│                         │
│                         │
│                         │
│                         │
├─────────────────────────┤
│ 홈  수주  (+)  재고  설정│  ← 하단 탭 바 (fixed)
└─────────────────────────┘
```

### 2.4 간격 (Spacing) 시스템

Tailwind 기본 스케일 사용. 커스텀 추가 없음.

```
컴포넌트 내부 패딩: p-3 (12px) ~ p-4 (16px)
카드 간 간격:       gap-4 (16px)
섹션 간 간격:       gap-6 (24px) ~ gap-8 (32px)
페이지 패딩:        px-4 (mobile), px-6 (tablet), px-8 (desktop)
```

---

## 3. 페이지별 레이아웃 & 와이어프레임

### 3.1 로그인 (/login)

```
┌─────────────────────────────────────────┐
│                                         │
│           ┌───────────────┐             │
│           │               │             │
│           │   CLOSET BIZ  │             │
│           │   ──────────  │             │
│           │               │             │
│           │  이메일        │             │
│           │  [___________] │             │
│           │               │             │
│           │  비밀번호      │             │
│           │  [___________] │             │
│           │               │             │
│           │  [  로그인  ]  │             │
│           │               │             │
│           └───────────────┘             │
│                                         │
│    bg-bg-primary, 중앙 카드 max-w-sm     │
└─────────────────────────────────────────┘
```

**컴포넌트 트리:**
```
LoginPage (Server Component)
└── LoginForm (Client Component)
    ├── Input[email]
    ├── Input[password]
    └── Button[submit]
```

**상태:** `react-hook-form` + `zod` 클라이언트 검증. Supabase Auth `signInWithPassword`.

---

### 3.2 대시보드 (/dashboard)

```
┌─────────────────────────────────────────────────────────────┐
│  대시보드                                    2026년 2월 14일 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │ 진행중    │ │ 이번달    │ │ 이번달    │ │ 재고 경고 │      │
│  │ 수주 7건  │ │ 매출     │ │ 매입     │ │  3 품목   │      │
│  │          │ │ ₩4,200만 │ │ ₩1,800만 │ │          │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
│                                                             │
│  ┌──────────────────────────┐ ┌──────────────────────────┐ │
│  │ 오늘의 일정               │ │ 수주 파이프라인           │ │
│  │ ─────────────────────── │ │ ──────────────────────── │ │
│  │ 09:00 수주 견적 - 마포 A씨│ │ 의뢰  견적  확정  실측    │ │
│  │ 14:00 이동 + 자재 준비    │ │  2    3    1    1      │ │
│  │ ─────────────────────── │ │ [수평 바 차트]           │ │
│  │ 내일: 휴무               │ │                         │ │
│  └──────────────────────────┘ └──────────────────────────┘ │
│                                                             │
│  ┌──────────────────────────┐ ┌──────────────────────────┐ │
│  │ 재고 경고                 │ │ 빠른 액션                │ │
│  │ ─────────────────────── │ │                          │ │
│  │ 🔴 앵글프레임 30T  2/10  │ │ [+ 신규 수주]            │ │
│  │ 🟡 선반 600mm     5/8   │ │ [+ 신규 발주]            │ │
│  │ 🔴 나사 M6        0/50  │ │ [📋 오늘 체크리스트]      │ │
│  └──────────────────────────┘ └──────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

**컴포넌트 트리:**
```
DashboardPage (Server Component - 병렬 데이터 로딩)
├── PageHeader title="대시보드"
├── DashboardGrid
│   ├── StatCard[진행중 수주] (Server)
│   ├── StatCard[이번달 매출] (Server)
│   ├── StatCard[이번달 매입] (Server)
│   └── StatCard[재고 경고] (Server)
├── Row (lg:grid-cols-2)
│   ├── TodayScheduleCard (Server)
│   │   └── ScheduleCard[] (각 일정)
│   └── OrderPipelineSummary (Server)
│       └── StatusBar (수평 바 차트)
└── Row (lg:grid-cols-2)
    ├── InventoryAlertList (Server)
    │   └── AlertItem[] (품목별)
    └── QuickActionBar (Client)
        └── Button[] (바로가기)
```

**데이터 패칭:**
- 각 카드는 독립된 Server Component로 `Suspense` 래핑
- 개별 로딩 스켈레톤 표시
- React Query로 클라이언트 리프레시 (60초 간격)

> **[M3] Dashboard 구조**: Dashboard는 KPI 카드의 auto-refresh (React Query `refetchInterval`)가 필요하므로 **Client Component**로 구현합니다. 초기 데이터는 부모 SC에서 prefetch하여 HydrationBoundary로 전달합니다.

---

### 3.3 수주 관리 (/orders)

#### 칸반 뷰 (Desktop/Tablet 기본)

```
┌─────────────────────────────────────────────────────────────┐
│  수주 관리   [검색...]    [필터 ▼]   [칸반] [목록] [타임라인]│
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ ┌─────────┐┌─────────┐┌─────────┐┌─────────┐┌─────────┐   │
│ │ 의뢰 (2)││ 견적 (3)││ 확정 (1)││ 실측 (1)││ 일자확정 ││   │
│ │─────────││─────────││─────────││─────────││─────────││   │
│ │┌───────┐││┌───────┐││┌───────┐││┌───────┐││         ││   │
│ ││강남 A씨│││송파 B씨│││마포 D씨│││역삼 F씨││         ││   │
│ ││시스템  │││앵글    │││혼합    │││시스템  ││         ││   │
│ ││₩180만 │││₩120만 │││₩350만 │││₩220만 ││         ││   │
│ ││2/12   │││2/10   │││2/15   │││2/18   ││         ││   │
│ │└───────┘││└───────┘││└───────┘││└───────┘││         ││   │
│ │┌───────┐││┌───────┐││         ││         ││         ││   │
│ ││서초 C씨│││분당 E씨││         ││         ││         ││   │
│ ││앵글    │││시스템  ││         ││         ││         ││   │
│ ││₩95만  │││₩200만 ││         ││         ││         ││   │
│ │└───────┘││└───────┘││         ││         ││         ││   │
│ │         ││┌───────┐││         ││         ││         ││   │
│ │         │││목동 G씨│││         ││         ││         ││   │
│ │         │││앵글    │││         ││         ││         ││   │
│ │         ││└───────┘││         ││         ││         ││   │
│ └─────────┘└─────────┘└─────────┘└─────────┘└─────────┘   │
│                                          ← 수평 스크롤 →   │
│                                                   [+ 수주] │
└─────────────────────────────────────────────────────────────┘
```

#### 리스트 뷰 (Mobile 기본)

```
┌─────────────────────────┐
│ ≡  수주 관리       🔍 + │
├─────────────────────────┤
│ [칸반▼]  [전체 상태 ▼]  │
├─────────────────────────┤
│                         │
│ ┌─────────────────────┐ │
│ │ 🟣 의뢰              │ │
│ │ 강남 A씨 · 시스템     │ │
│ │ ₩180만 · 2/12        │ │
│ │               [→ 견적]│ │  ← 다음 상태 전환 버튼
│ └─────────────────────┘ │
│                         │
│ ┌─────────────────────┐ │
│ │ 🔵 견적              │ │
│ │ 송파 B씨 · 앵글       │ │
│ │ ₩120만 · 2/10        │ │
│ │               [→ 확정]│ │
│ └─────────────────────┘ │
│                         │
│ ┌─────────────────────┐ │
│ │ 🟢 확정              │ │
│ │ 마포 D씨 · 혼합       │ │
│ │ ₩350만 · 2/15        │ │
│ │               [→ 실측]│ │
│ └─────────────────────┘ │
│         ...             │
│                         │
│                  [+ 수주]│ ← FAB
└─────────────────────────┘
```

**컴포넌트 트리:**
```
OrdersPage (Server Component)
├── PageHeader
│   ├── title="수주 관리"
│   ├── SearchInput
│   ├── FilterDropdown (상태, 기간, 옷장 유형)
│   └── ResponsiveViewSwitcher[kanban, list, timeline]
├── ViewContainer[currentView]
│   ├── (kanban) OrderKanbanView (Client)
│   │   └── KanbanColumn[] (@dnd-kit/react DragDropProvider)
│   │       └── OrderCard[] (Draggable)
│   ├── (list) OrderListView (Client)
│   │   └── OrderListItem[]
│   │       ├── StatusBadge
│   │       ├── CustomerBrief
│   │       ├── PriceDisplay
│   │       └── StatusTransitionButton
│   └── (timeline) OrderTimelineView (Client)
│       └── TimelineNode[]
└── NewOrderFAB (모바일) / NewOrderButton (데스크탑)
    └── (모바일) NewOrderBottomSheet
```

**상태 관리:**
```typescript
// 수주 목록 - Server State
const { data, isLoading } = useQuery({
  queryKey: ['orders', { status, search, dateRange }],
  queryFn: () => fetchOrders({ status, search, dateRange }),
});

// 뷰 상태 - Client State (localStorage)
const [view, setView] = useViewState({
  storageKey: 'closetbiz-orders-view',
  defaultView: { mobile: 'list', tablet: 'kanban', desktop: 'kanban' },
});

// 필터 상태 - URL State (searchParams)
// /orders?status=confirmed&q=강남
```

---

### 3.4 수주 상세 (/orders/[id])

```
┌─────────────────────────────────────────────────────────────┐
│  ← 뒤로   CB-2026-0005 · 마포 D씨           [수정] [취소]  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 상태: 🟢 확정                          [→ 실측 완료]  │  │  ← 다음 상태 전환
│  │ ──────────────────────────────────────────────────── │  │
│  │ 의뢰 → 견적 → [확정] → 실측 → 일자 → 준비 → 설치    │  │  ← 상태 프로그레스
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌─────────────────────────┐ ┌─────────────────────────┐   │
│  │ 고객 정보                │ │ 옷장 사양               │   │
│  │ ───────────────────── │ │ ────────────────────── │   │
│  │ 마포 D씨               │ │ 유형: 혼합               │   │
│  │ 010-1234-5678         │ │ 규격: 2400×2100×600mm   │   │
│  │ 마포구 동교동 123-45   │ │ 구성: 선반 6, 행거 2    │   │
│  └─────────────────────────┘ └─────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────┐ ┌─────────────────────────┐   │
│  │ 금액 정보                │ │ 일정                    │   │
│  │ ───────────────────── │ │ ────────────────────── │   │
│  │ 견적: ₩3,500,000      │ │ 실측: 2/15 (금)         │   │
│  │ 확정: ₩3,200,000      │ │ 설치: 미정               │   │
│  │ 결제: 미정              │ │                         │   │
│  └─────────────────────────┘ └─────────────────────────┘   │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 자재 목록                                              │  │
│  │ ──────────────────────────────────────────────────── │  │
│  │ 품목           계획  hold  부족   상태                 │  │
│  │ 앵글프레임 30T   8     8     0    ✅                   │  │
│  │ 선반 600mm      6     5     1    ⚠️ 부족              │  │
│  │ 행거봉 900mm    2     2     0    ✅                   │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 변경 이력                                              │  │
│  │ ──────────────────────────────────────────────────── │  │
│  │ ● 2/12 14:30  확정 (확정금액 ₩3,200,000)              │  │
│  │ ● 2/10 09:15  견적 발송 (견적금액 ₩3,500,000)          │  │
│  │ ● 2/08 16:20  의뢰 등록                                │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  [체크리스트]  [2D/3D 모델]  [견적서 PDF]                    │
└─────────────────────────────────────────────────────────────┘
```

**컴포넌트 트리:**
```
OrderDetailPage (Server Component)
├── PageHeader (뒤로 + 수주번호 + 액션)
├── OrderStatusBar (Client)
│   ├── StatusBadge
│   ├── StatusProgressIndicator (9단계 프로그레스)
│   └── StatusTransitionButton
├── InfoGrid (lg:grid-cols-2)
│   ├── CustomerInfoCard (Server)
│   ├── ClosetSpecCard (Server)
│   ├── FinanceInfoCard (Server)
│   └── ScheduleInfoCard (Server)
├── OrderMaterialsTable (Client)
│   └── MaterialRow[]
│       ├── InventoryGauge
│       └── StatusBadge[shortage]
├── OrderTimeline (Server)
│   └── TimelineEntry[]
└── ActionBar
    ├── Link[checklist]
    ├── Link[model]
    └── Button[generate-pdf]
```

---

### 3.5 수주 등록/수정 (/orders/new, /orders/[id]/edit)

```
┌─────────────────────────────────────────────────────────────┐
│  ← 뒤로   신규 수주 등록                           [저장]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 고객 정보                                               │
│  ┌──────────────────────────┐ ┌──────────────────────────┐ │
│  │ 고객 선택/검색            │ │ [+ 신규 고객 등록]        │ │
│  │ [고객명 검색...]         │ │                           │ │
│  └──────────────────────────┘ └──────────────────────────┘ │
│                                                             │
│  2. 옷장 사양                                               │
│  ┌──────────────────────────┐ ┌──────────────────────────┐ │
│  │ 유형                      │ │ 현장 주소                 │ │
│  │ ○ 앵글 ○ 시스템 ○ 혼합    │ │ [___________________]    │ │
│  └──────────────────────────┘ └──────────────────────────┘ │
│  ┌──────────────────────────┐ ┌────────┐ ┌────────┐       │
│  │ 너비 (mm)                 │ │ 높이   │ │ 깊이   │       │
│  │ [   2400   ]  [-] [+]   │ │ [2100] │ │ [600]  │       │
│  └──────────────────────────┘ └────────┘ └────────┘       │
│                                                             │
│  3. 견적 정보                                               │
│  ┌──────────────────────────┐                               │
│  │ 견적 금액                  │                               │
│  │ [     ₩3,500,000     ]   │                               │
│  └──────────────────────────┘                               │
│                                                             │
│  4. 메모                                                    │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ [                                                    ]  │
│  │ [                                                    ]  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  [임시저장]                                        [저장]   │
└─────────────────────────────────────────────────────────────┘
```

**폼 상태 관리:**
```typescript
// react-hook-form + zod
const orderSchema = z.object({
  customerId: z.string().uuid().optional(),
  customerName: z.string().min(1, '고객명을 입력하세요'),
  closetType: z.enum(['angle', 'system', 'mixed']),
  closetSpec: closetSpecSchema,
  quotationAmount: z.number().min(0),
  siteAddress: z.string().optional(),
  memo: z.string().optional(),
});

const form = useForm<OrderFormValues>({
  resolver: zodResolver(orderSchema),
  defaultValues: existingOrder ?? defaultValues,
});
```

---

### 3.6 고객 관리 (/customers)

```
┌─────────────────────────────────────────────────────────────┐
│  고객 관리    [검색...]                         [+ 고객 등록]│
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 이름        연락처          주소         수주     매출  │  │
│  │ ────────── ────────────── ─────────── ───── ────── │  │
│  │ 강남 A씨   010-1234-5678  강남구 역삼동  3건  ₩560만 │  │
│  │ 서초 C씨   010-2345-6789  서초구 서초동  1건  ₩95만  │  │
│  │ 마포 D씨   010-3456-7890  마포구 동교동  2건  ₩520만 │  │
│  │ ...                                                  │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                  1/3 페이지 │
└─────────────────────────────────────────────────────────────┘
```

**모바일 레이아웃:**
```
┌─────────────────────────┐
│ ≡  고객 관리       🔍 + │
├─────────────────────────┤
│                         │
│ ┌─────────────────────┐ │
│ │ 강남 A씨             │ │
│ │ 📱 010-1234-5678    │ │
│ │ 📍 강남구 역삼동      │ │
│ │ 수주 3건 · ₩560만    │ │
│ └─────────────────────┘ │
│                         │
│ ┌─────────────────────┐ │
│ │ 서초 C씨             │ │
│ │ 📱 010-2345-6789    │ │
│ │ 수주 1건 · ₩95만     │ │
│ └─────────────────────┘ │
│          ...            │
└─────────────────────────┘
```

---

### 3.7 재고 관리 (/inventory)

#### 그리드 뷰 (Desktop/Tablet)

```
┌─────────────────────────────────────────────────────────────┐
│  재고 관리  [검색...] [카테고리 ▼] [상태 ▼]  [그리드] [목록]│
├─────────────────────────────────────────────────────────────┤
│  ⚠️ 재고 부족 3건: 앵글프레임 30T, 나사 M6, 선반 600mm      │  ← sticky 배너
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │ 앵글프레임 │ │ 시스템    │ │ 선반      │ │ 행거봉    │      │
│  │ 30T       │ │ 프레임    │ │ 600mm    │ │ 900mm    │      │
│  │           │ │           │ │           │ │           │      │
│  │ ████░░░░ │ │ ████████ │ │ █████░░░ │ │ ████████ │      │
│  │  2/10  🔴│ │  25/10 ✅│ │  5/8   🟡│ │  12/5  ✅│      │
│  │           │ │           │ │           │ │           │      │
│  │ hold: 3   │ │ hold: 0   │ │ hold: 2   │ │ hold: 0   │      │
│  │ 가용: -1  │ │ 가용: 25  │ │ 가용: 3   │ │ 가용: 12  │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
│                                                             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │ 서랍      │ │ 나사 M6   │ │ 브라켓    │ │ 엣지밴드  │      │
│  │ 3단       │ │           │ │ L형      │ │ 화이트    │      │
│  │           │ │           │ │           │ │           │      │
│  │ ████████ │ │ ░░░░░░░░ │ │ ████████ │ │ ████████ │      │
│  │  8/3   ✅│ │  0/50  🔴│ │  40/10 ✅│ │  15/5  ✅│      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
│                                                   [+ 자재] │
└─────────────────────────────────────────────────────────────┘
```

**InventoryCard 컴포넌트 구조:**
```typescript
interface InventoryCardProps {
  product: Product;
  inventory: Inventory;
}

// 색상 로직
// 🔴 critical: available_quantity < 0 또는 quantity < min_stock * 0.3
// 🟡 warning:  quantity < min_stock
// ✅ sufficient: quantity >= min_stock
```

---

### 3.8 발주 관리 (/purchases)

```
┌─────────────────────────────────────────────────────────────┐
│  발주 관리  [검색...]  [전체 | 대기 | 완료 | 입고]  [+ 발주]│
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 번호         공급처      상태      금액     발주일     │  │
│  │ ────────── ────────── ──────── ──────── ────────── │  │
│  │ PO-2026-0003  ㈜금성철강  🟢 입고    ₩850만   2/08   │  │
│  │ PO-2026-0002  대한목재    🔵 발주    ₩120만   2/05   │  │
│  │ PO-2026-0001  ㈜금성철강  ⚫ 정산    ₩430만   1/28   │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

### 3.9 매출/매입 (/finance)

#### 요약 뷰 (기본)

```
┌─────────────────────────────────────────────────────────────┐
│  매출/매입   [2026년 2월 ▼]          [요약] [상세] [캘린더]  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐       │
│  │ 이번달 매출    │ │ 이번달 매입    │ │ 순이익        │       │
│  │ ₩4,200,000   │ │ ₩1,800,000   │ │ ₩2,400,000   │       │
│  │ ▲ +15% 전월비 │ │ ▼ -5% 전월비  │ │ ▲ +28% 전월비 │       │
│  └──────────────┘ └──────────────┘ └──────────────┘       │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              월별 매출/매입 추이                        │  │
│  │                                                       │  │
│  │  ₩500만 ┤                                              │  │
│  │         │    ██                                        │  │
│  │  ₩400만 ┤    ██  ██                         ██         │  │
│  │         │ ██ ██  ██              ██  ██      ██         │  │
│  │  ₩300만 ┤ ██ ██  ██  ██         ██  ██  ██  ██         │  │
│  │         │ ██ ██  ██  ██  ██     ██  ██  ██  ██         │  │
│  │  ₩200만 ┤ ██ ██  ██  ██  ██  ██ ██  ██  ██  ██         │  │
│  │         └────────────────────────────────────────       │  │
│  │           9월 10월 11월 12월 1월  2월                     │  │
│  │         ██ 매출  ▓▓ 매입                                │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌─────────────────────────┐ ┌─────────────────────────┐   │
│  │ 미정산 수주 (2건)         │ │ 미정산 발주 (1건)        │   │
│  │ CB-2026-0007 ₩220만     │ │ PO-2026-0002 ₩120만     │   │
│  │ CB-2026-0005 ₩320만     │ │                          │   │
│  └─────────────────────────┘ └─────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

### 3.10 설정 (/settings)

```
┌─────────────────────────────────────────────────────────────┐
│  설정                                                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  프로필                                                     │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 사업자명    [___________________]                      │  │
│  │ 이메일      user@example.com (변경 불가)                │  │
│  │ 연락처      [010-0000-0000     ]                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  테마                                                       │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 화면 모드   ○ 라이트  ● 다크  ○ 시스템 설정              │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  데이터                                                     │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ [CSV 내보내기]   [데이터 백업]                          │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  계정                                                       │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ [비밀번호 변경]  [로그아웃]                              │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 상태 관리 전략

### 4.0 Server → Client Hydration 패턴 [M2]

Server Component에서 prefetch한 데이터를 Client Component의 React Query로 넘기는 패턴.

```typescript
// app/(app)/orders/page.tsx (Server Component)
import { dehydrate, HydrationBoundary, QueryClient } from '@tanstack/react-query';
import { createServerClient } from '@/lib/supabase/server';
import { OrdersView } from '@/components/orders/orders-view';

export default async function OrdersPage() {
  const queryClient = new QueryClient();
  const supabase = await createServerClient();

  // 서버에서 prefetch
  await queryClient.prefetchQuery({
    queryKey: ['orders'],
    queryFn: async () => {
      const { data } = await supabase
        .from('orders')
        .select('*, customers(name, phone)')
        .order('created_at', { ascending: false });
      return data;
    },
  });

  return (
    <HydrationBoundary state={dehydrate(queryClient)}>
      <OrdersView />  {/* Client Component - useQuery로 동일 키 사용 */}
    </HydrationBoundary>
  );
}
```

> **원칙**: Server Component에서 `prefetchQuery` → `HydrationBoundary`로 감싸기 → Client Component에서 동일 queryKey로 `useQuery` 호출. 초기 데이터가 즉시 사용 가능하며 이후 클라이언트에서 refetch.

### 4.1 Server Components vs Client Components 경계

```
Server Components (기본):
├── 페이지 레이아웃 (page.tsx, layout.tsx)
├── 데이터 표시 전용 카드 (StatCard, InfoCard)
├── 정적 리스트/테이블
└── SEO가 필요한 콘텐츠

Client Components ('use client'):
├── 인터랙션이 있는 모든 것
│   ├── 폼 (react-hook-form)
│   ├── 드래그&드롭 (칸반, 캘린더)
│   ├── 뷰 전환 (ViewSwitcher)
│   ├── 테마 전환 (ThemeToggle)
│   ├── 모달/다이얼로그
│   └── 검색/필터
├── 브라우저 API 의존
│   ├── localStorage (뷰 상태, 테마)
│   ├── matchMedia (반응형)
│   └── Service Worker (PWA)
└── Three.js 에디터 (dynamic import, ssr: false)
```

### 4.2 데이터 패칭 패턴

```typescript
// 패턴 1: Server Component 직접 페칭 (초기 로드)
// app/(app)/orders/page.tsx
export default async function OrdersPage() {
  const supabase = await createServerClient();
  const { data: orders } = await supabase
    .from('orders')
    .select('*, customers(name, phone)')
    .order('created_at', { ascending: false });

  return <OrdersContent initialOrders={orders} />;
}

// 패턴 2: React Query (클라이언트 리페칭/뮤테이션)
// hooks/use-orders.ts
export function useOrders(filters: OrderFilters) {
  return useQuery({
    queryKey: ['orders', filters],
    queryFn: () => fetchOrders(filters),
    staleTime: 30_000,        // 30초간 fresh
    refetchOnWindowFocus: true,
  });
}

export function useUpdateOrderStatus() {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: updateOrderStatus,
    onMutate: async (variables) => {
      // Optimistic update (칸반 드래그)
      await queryClient.cancelQueries({ queryKey: ['orders'] });
      const previous = queryClient.getQueryData(['orders']);
      queryClient.setQueryData(['orders'], (old) => /* optimistic */);
      return { previous };
    },
    onError: (err, variables, context) => {
      // Rollback
      queryClient.setQueryData(['orders'], context?.previous);
    },
    onSettled: () => {
      queryClient.invalidateQueries({ queryKey: ['orders'] });
    },
  });
}

// 패턴 3: Server Action (폼 제출)
// actions/orders.ts
'use server';
export async function createOrder(data: OrderFormValues) {
  const supabase = await createServerClient();
  const { data: user } = await supabase.auth.getUser();
  // ... validation, insert, return
}
```

### 4.3 캐시 전략

```typescript
// lib/query-config.ts
export const queryConfig = {
  orders: {
    staleTime: 30_000,       // 30초
    gcTime: 5 * 60_000,      // 5분
  },
  inventory: {
    staleTime: 60_000,       // 1분 (변동 빈도 낮음)
    gcTime: 10 * 60_000,     // 10분
  },
  customers: {
    staleTime: 5 * 60_000,   // 5분 (거의 안 바뀜)
    gcTime: 30 * 60_000,     // 30분
  },
  dashboard: {
    staleTime: 60_000,       // 1분
    refetchInterval: 60_000, // 자동 리프레시
  },
};
```

### 4.4 폼 상태 패턴

모든 폼은 `react-hook-form` + `zod` 사용.

```typescript
// 공통 폼 패턴
function EntityForm({ defaultValues, onSubmit, mode }: FormProps) {
  const form = useForm({
    resolver: zodResolver(schema),
    defaultValues,
  });

  return (
    <Form {...form}>
      <form onSubmit={form.handleSubmit(onSubmit)}>
        <FormField control={form.control} name="fieldName" render={...} />
        {/* ... */}
        <Button type="submit" disabled={form.formState.isSubmitting}>
          {form.formState.isSubmitting ? <Spinner /> : '저장'}
        </Button>
      </form>
    </Form>
  );
}
```

### 4.5 URL 상태 (필터/검색)

검색/필터는 URL searchParams로 관리. 새로고침/공유 가능.

```typescript
// hooks/use-search-params-state.ts
export function useFilterParams() {
  const searchParams = useSearchParams();
  const router = useRouter();
  const pathname = usePathname();

  const setFilter = useCallback((key: string, value: string | null) => {
    const params = new URLSearchParams(searchParams);
    if (value) params.set(key, value);
    else params.delete(key);
    router.replace(`${pathname}?${params.toString()}`);
  }, [searchParams, router, pathname]);

  return { searchParams, setFilter };
}
```

---

## 5. 컬러칩 정의

이 섹션은 개발자가 구현 시 참조하는 실용 가이드다. 전체 토큰 정의는 `dark-mode-multiview-ux-design.md` 1.1절에 있으며, 여기서는 **"언제 어떤 색을 쓰는가"**를 정의한다.

### 5.1 표면 (Surfaces)

| 용도 | Tailwind Class | Light | Dark | 사용처 |
|------|---------------|-------|------|--------|
| 페이지 배경 | `bg-bg-primary` | `#FAFAFA` | `#0A0A0A` | body, 전체 배경 |
| 카드/패널 | `bg-bg-secondary` | `#FFFFFF` | `#171717` | Card, 사이드바, 입력 필드 배경 |
| 호버/선택 | `bg-bg-tertiary` | `#F5F5F5` | `#262626` | 리스트 아이템 hover, 선택된 탭 |
| 모달/팝오버 | `bg-bg-elevated` | `#FFFFFF` | `#1F1F1F` | Dialog, DropdownMenu, Sheet |

### 5.2 텍스트 (Foreground)

| 용도 | Tailwind Class | Light | Dark | 사용처 |
|------|---------------|-------|------|--------|
| 주요 텍스트 | `text-fg-primary` | `#0A0A0A` | `#FAFAFA` | 제목, 본문, 중요 데이터 |
| 보조 텍스트 | `text-fg-secondary` | `#525252` | `#A3A3A3` | 부가 설명, 날짜, 메타 정보 |
| 비활성 텍스트 | `text-fg-tertiary` | `#A3A3A3` | `#525252` | placeholder, disabled, 힌트 |
| 반전 텍스트 | `text-fg-inverted` | `#FFFFFF` | `#0A0A0A` | 채워진 버튼 위 텍스트 |

### 5.3 테두리 (Borders)

| 용도 | Tailwind Class | Light | Dark | 사용처 |
|------|---------------|-------|------|--------|
| 기본 테두리 | `border-border-default` | `#E5E5E5` | `#262626` | 카드, 입력 필드, 구분선 |
| 강조 테두리 | `border-border-strong` | `#D4D4D4` | `#404040` | 포커스 주변, 활성 상태 |
| 미세 테두리 | `border-border-subtle` | `#F5F5F5` | `#171717` | 내부 구분선, 그룹 내 분리 |

### 5.4 브랜드 (Brand)

| 용도 | Tailwind Class | Light | Dark | 사용처 |
|------|---------------|-------|------|--------|
| Primary | `bg-primary` / `text-primary` | `#0F172A` | `#F1F5F9` | 주요 버튼, 활성 네비, 링크 |
| Primary hover | `bg-primary-hover` | `#1E293B` | `#E2E8F0` | 주요 버튼 hover |
| Primary 배경 | `bg-primary-subtle` | `#F1F5F9` | `#1E293B` | 선택된 항목 배경, 태그 배경 |
| Accent | `bg-accent` / `text-accent` | `#F59E0B` | `#FBBF24` | CTA 강조, 오늘 날짜, 중요 표시 |
| Accent hover | `bg-accent-hover` | `#D97706` | `#FCD34D` | Accent 버튼 hover |
| Accent 배경 | `bg-accent-subtle` | `#FEF3C7` | `#451A03` | Accent 항목 배경 |

### 5.5 상태 (Status)

| 상태 | Tailwind Class | Light | Dark | 사용처 |
|------|---------------|-------|------|--------|
| 성공 | `text-success` / `bg-success-bg` | `#059669` / `#D1FAE5` | `#10B981` / `#064E3B` | 완료, 충분 재고, 성공 알림 |
| 경고 | `text-warning` / `bg-warning-bg` | `#D97706` / `#FEF3C7` | `#FBBF24` / `#451A03` | 재고 부족 임박, 주의 |
| 위험 | `text-danger` / `bg-danger-bg` | `#DC2626` / `#FEE2E2` | `#EF4444` / `#450A0A` | 재고 부족, 에러, 삭제, 취소 |
| 정보 | `text-info` / `bg-info-bg` | `#2563EB` / `#DBEAFE` | `#3B82F6` / `#1E3A8A` | 안내, 팁, 정보 배지 |

### 5.6 수주 상태 컬러칩 (10단계)

수주 파이프라인 전용 색상. 칸반 컬럼 헤더, StatusBadge, 타임라인 노드에 사용.

| 상태 | 한국어 | Tailwind Class | Light | Dark | 칩 스타일 |
|------|--------|---------------|-------|------|-----------|
| `inquiry` | 의뢰 | `bg-order-inquiry` | `#8B5CF6` | `#A78BFA` | 텍스트: white |
| `quotation_sent` | 견적 | `bg-order-quotation-sent` | `#3B82F6` | `#60A5FA` | 텍스트: white |
| `confirmed` | 확정 | `bg-order-confirmed` | `#10B981` | `#34D399` | 텍스트: white |
| `measurement_done` | 실측 | `bg-order-measurement-done` | `#14B8A6` | `#2DD4BF` | 텍스트: white |
| `date_fixed` | 일자확정 | `bg-order-date-fixed` | `#F59E0B` | `#FBBF24` | 텍스트: black |
| `material_held` | 준비 | `bg-order-material-held` | `#06B6D4` | `#22D3EE` | 텍스트: white |
| `installed` | 설치 | `bg-order-installed` | `#EC4899` | `#F472B6` | 텍스트: white |
| `settlement_wait` | 정산대기 | `bg-order-settlement-wait` | `#F97316` | `#FB923C` | 텍스트: white |
| `revenue_confirmed` | 매출확정 | `bg-order-revenue-confirmed` | `#059669` | `#10B981` | 텍스트: white |
| `cancelled` | 취소 | `bg-order-cancelled` | `#6B7280` | `#9CA3AF` | 텍스트: white |

**StatusBadge 렌더링 규칙:**

```
┌──────────────────────────────────────────────────┐
│                                                  │
│  ● 의뢰    ● 견적    ● 확정    ● 실측    ● 일자  │
│  #8B5CF6   #3B82F6   #10B981   #14B8A6   #F59E0B │
│                                                  │
│  ● 준비    ● 설치    ● 정산    ● 완료    ● 취소  │
│  #06B6D4   #EC4899   #F97316   #059669   #6B7280 │
│                                                  │
└──────────────────────────────────────────────────┘
```

칩 형태:
```
[● 의뢰]  = 배경 bg-order-inquiry/10 + 텍스트 text-order-inquiry + 좌측 dot ●
[● 취소]  = 배경 bg-order-cancelled/10 + 텍스트 text-order-cancelled + 좌측 dot ●
```

```typescript
// lib/constants/order-status.ts
export const ORDER_STATUS_CHIP: Record<OrderStatus, {
  label: string;
  colorClass: string;      // Tailwind text-* class
  bgClass: string;         // Tailwind bg-*/10 (10% opacity 배경)
  dotClass: string;        // Tailwind bg-* (dot 색상)
}> = {
  inquiry:           { label: '의뢰',     colorClass: 'text-order-inquiry',          bgClass: 'bg-order-inquiry/10',          dotClass: 'bg-order-inquiry' },
  quotation_sent:    { label: '견적',     colorClass: 'text-order-quotation-sent',   bgClass: 'bg-order-quotation-sent/10',   dotClass: 'bg-order-quotation-sent' },
  confirmed:         { label: '확정',     colorClass: 'text-order-confirmed',        bgClass: 'bg-order-confirmed/10',        dotClass: 'bg-order-confirmed' },
  measurement_done:  { label: '실측',     colorClass: 'text-order-measurement-done', bgClass: 'bg-order-measurement-done/10', dotClass: 'bg-order-measurement-done' },
  date_fixed:        { label: '일자확정', colorClass: 'text-order-date-fixed',       bgClass: 'bg-order-date-fixed/10',       dotClass: 'bg-order-date-fixed' },
  material_held:     { label: '준비',     colorClass: 'text-order-material-held',    bgClass: 'bg-order-material-held/10',    dotClass: 'bg-order-material-held' },
  installed:         { label: '설치',     colorClass: 'text-order-installed',        bgClass: 'bg-order-installed/10',        dotClass: 'bg-order-installed' },
  settlement_wait:   { label: '정산대기', colorClass: 'text-order-settlement-wait',  bgClass: 'bg-order-settlement-wait/10',  dotClass: 'bg-order-settlement-wait' },
  revenue_confirmed: { label: '매출확정', colorClass: 'text-order-revenue-confirmed', bgClass: 'bg-order-revenue-confirmed/10', dotClass: 'bg-order-revenue-confirmed' },
  cancelled:         { label: '취소',     colorClass: 'text-order-cancelled',        bgClass: 'bg-order-cancelled/10',        dotClass: 'bg-order-cancelled' },
};
```

### 5.7 재고 상태 컬러칩

| 상태 | 조건 | 배경 | 테두리 | 텍스트 | Tailwind |
|------|------|------|--------|--------|----------|
| 충분 | `quantity >= min_stock` | `#D1FAE5` / `#064E3B` | `#6EE7B7` / `#047857` | `#059669` / `#10B981` | `bg-success-bg border-success text-success` |
| 주의 | `quantity < min_stock && quantity > 0` | `#FEF3C7` / `#451A03` | `#FCD34D` / `#D97706` | `#D97706` / `#FBBF24` | `bg-warning-bg border-warning text-warning` |
| 부족 | `quantity <= 0 또는 available < 0` | `#FEE2E2` / `#450A0A` | `#FCA5A5` / `#DC2626` | `#DC2626` / `#EF4444` | `bg-danger-bg border-danger text-danger` |

```
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│  ✅ 충분      │ │  ⚠️ 주의      │ │  🔴 부족      │
│  ████████░░  │ │  █████░░░░░  │ │  █░░░░░░░░░  │
│  25/10       │ │  5/8         │ │  0/50        │
│  bg-success  │ │  bg-warning  │ │  bg-danger   │
└──────────────┘ └──────────────┘ └──────────────┘
```

### 5.8 발주 상태 컬러칩

| 상태 | 한국어 | 색상 (Light/Dark) | 사용 |
|------|--------|-------------------|------|
| `draft` | 작성중 | `#6B7280` / `#9CA3AF` (Gray) | 리스트 배지 |
| `ordered` | 발주완료 | `#3B82F6` / `#60A5FA` (Blue) | 리스트 배지 |
| `received` | 입고완료 | `#10B981` / `#34D399` (Green) | 리스트 배지 |
| `settled` | 정산완료 | `#8B5CF6` / `#A78BFA` (Violet) | 리스트 배지 |
| `cost_confirmed` | 매입확정 | `#059669` / `#10B981` (Emerald) | 리스트 배지 |

### 5.9 스케줄 유형 컬러칩

캘린더/어젠다에서 일정 유형을 시각적으로 구분.

| 유형 | 한국어 | 색상 (Light/Dark) | 캘린더 블록 |
|------|--------|-------------------|-------------|
| `measurement` | 실측 | `#3B82F6` / `#60A5FA` (Blue) | 파란 블록 |
| `installation` | 설치 | `#EC4899` / `#F472B6` (Pink) | 분홍 블록 |
| `visit` | 방문/검수 | `#F97316` / `#FB923C` (Orange) | 주황 블록 |
| `delivery` | 입고/배송 | `#6B7280` / `#9CA3AF` (Gray) | 회색 블록 |
| `other` | 기타 | `#F59E0B` / `#FBBF24` (Amber) | 노란 블록 |

### 5.10 컬러 적용 원칙

| 원칙 | 설명 |
|------|------|
| **시맨틱 토큰만 사용** | `bg-blue-500` 같은 Tailwind 기본 색상 직접 사용 금지. 반드시 `bg-order-quotation-sent`, `text-success` 등 시맨틱 클래스 사용 |
| **`dark:` prefix 금지** | CSS variables로 자동 전환하므로 `dark:bg-gray-900` 같은 직접 지정 불필요 |
| **투명도로 배경 생성** | 상태 칩 배경은 `bg-order-inquiry/10` (10% 투명도) 패턴. 별도 배경색 토큰 불필요 |
| **대비 검증 필수** | 새 색상 조합 추가 시 WCAG AA (4.5:1) 검증. 특히 다크모드에서 재확인 |
| **차트 색상 = 수주 상태 색상** | 차트 팔레트를 별도 정의하지 않고 수주/재고 상태 색상을 그대로 재사용 |

---

## 6. CSS 아키텍처

### 6.1 Tailwind 커스텀 설정 (색상 외)

**색상/테마**: `dark-mode-multiview-ux-design.md` 1.2절 CSS Variables 참조.

**추가 유틸리티:**

```css
/* globals.css - 프로젝트 커스텀 유틸리티 */

@layer utilities {
  /* 현장 장갑 대응: 큰 터치 영역 */
  .touch-target {
    @apply min-h-[44px] min-w-[44px];
  }

  .touch-target-lg {
    @apply min-h-[56px] min-w-[56px];
  }

  /* 숫자 표시: tabular numerals */
  .tabular-nums {
    font-variant-numeric: tabular-nums;
    font-family: var(--font-mono);
  }

  /* 한줄 말줄임 */
  .truncate-1 {
    @apply truncate;
  }

  /* 두줄 말줄임 */
  .truncate-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  /* 스크롤바 숨김 (칸반 수평 스크롤) */
  .scrollbar-hide {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
  .scrollbar-hide::-webkit-scrollbar {
    display: none;
  }
}
```

### 6.2 shadcn/ui 커스터마이징 규칙

shadcn/ui는 코드를 소유하는 방식이므로, `components/ui/` 내 파일을 프로젝트에 맞게 **직접 수정해도 된다.**

**커스터마이징 레벨:**

| 레벨 | 방법 | 예시 |
|------|------|------|
| **L1: 테마 변수** | `globals.css` CSS variables 수정 | 색상, 반지름, 그림자 |
| **L2: variant 추가** | `button.tsx` 등에 variant 직접 추가 | `variant: "status-success"` |
| **L3: 기본값 변경** | default props, 기본 className 수정 | 모든 Button에 `touch-target` 적용 |
| **L4: 구조 변경** | 컴포넌트 내부 구조 수정 | Badge에 StatusDot 내장 |

**원칙:**
1. shadcn CLI로 추가한 후 프로젝트에 맞게 수정 (코드 소유)
2. 도메인 로직은 `components/{domain}/`에서 조합 (Molecules/Organisms)
3. `components/ui/`는 도메인 무관한 범용 UI만 담당

```typescript
// L2 예시: Button에 프로젝트 전용 variant 추가
// components/ui/button.tsx 직접 수정
const buttonVariants = cva("...", {
  variants: {
    variant: {
      default: "...",
      destructive: "...",
      // 프로젝트 전용 추가
      "status-action": "bg-accent text-accent-contrast hover:bg-accent-hover touch-target",
    },
  },
});

// Organism에서 사용
// components/orders/status-transition-button.tsx
import { Button } from '@/components/ui/button';

export function StatusTransitionButton({ nextStatus, onTransition }: Props) {
  return (
    <Button variant="status-action" size="sm" className="gap-2" onClick={() => onTransition(nextStatus)}>
      <ArrowRight className="h-4 w-4" />
      {ORDER_STATUS_LABELS[nextStatus]}
    </Button>
  );
}
```

### 6.3 컴포넌트 스타일 규칙

| 항목 | 규칙 |
|------|------|
| 간격 | Tailwind spacing scale만 사용 (임의값 지양) |
| 색상 | 시맨틱 토큰만 사용 (`bg-bg-primary`, `text-fg-secondary`) |
| 다크모드 | CSS variables로 자동 전환. `dark:` prefix 직접 사용 금지 |
| 반응형 | `sm:`, `md:`, `lg:`, `xl:` prefix. Mobile-first |
| 포커스 | `focus-visible:ring-2 focus-visible:ring-accent` |
| 호버 | `hover:bg-bg-tertiary` (표면), `hover:text-primary-hover` (텍스트) |
| 전환 | `transition-colors duration-150` (색상), `transition-all duration-200` (레이아웃) |

---

## 7. 접근성 (Accessibility)

### 7.1 WCAG AA 준수 체크리스트

| 항목 | 기준 | 구현 방법 |
|------|------|-----------|
| **색상 대비** | 일반 텍스트 4.5:1, 대형 텍스트 3:1 | 디자인 토큰에서 사전 검증 완료 |
| **키보드 네비게이션** | 모든 인터랙션 키보드 접근 가능 | Radix UI 기본 제공 + 커스텀 핸들러 |
| **포커스 표시** | 포커스 상태 시각적 구분 | `focus-visible:ring-2` |
| **터치 타겟** | 최소 44x44px | `.touch-target` 유틸리티 |
| **스크린 리더** | 의미 있는 라벨, 상태 알림 | `aria-label`, `aria-live`, `sr-only` |
| **모션** | 애니메이션 축소 옵션 | `prefers-reduced-motion` 지원 |

### 7.2 키보드 네비게이션 패턴

```
전역:
- Tab/Shift+Tab: 포커스 이동
- Enter/Space: 버튼/링크 활성화
- Escape: 모달/다이얼로그 닫기

칸반 보드:
- Arrow Left/Right: 컬럼 간 이동
- Arrow Up/Down: 같은 컬럼 내 카드 이동
- Enter: 카드 상세 열기
- Space: 드래그 모드 진입 (@dnd-kit/react keyboard sensor)

테이블:
- Arrow keys: 셀 간 이동
- Enter: 행 상세/편집

캘린더:
- Arrow keys: 날짜 이동
- Enter: 일정 생성/열기
- Page Up/Down: 월 이동
```

### 7.3 ARIA 패턴

```typescript
// 상태 변경 알림
<div aria-live="polite" className="sr-only">
  {statusChanged && `수주 상태가 ${ORDER_STATUS_LABELS[newStatus]}(으)로 변경되었습니다.`}
</div>

// 재고 경고 배너
<AlertBanner
  role="alert"
  aria-label={`재고 부족 경고: ${count}개 품목`}
/>

// 칸반 컬럼
<div role="region" aria-label={`${statusLabel} - ${count}건`}>

// 뷰 전환
<div role="tablist" aria-label="보기 전환">
  <button role="tab" aria-selected={isActive} aria-controls={panelId}>
```

---

## 8. 로딩/에러/빈 상태 패턴

### 8.1 로딩 상태 (Skeleton)

각 뷰 타입별 전용 스켈레톤.

```typescript
// 칸반 스켈레톤
function KanbanSkeleton() {
  return (
    <div className="flex gap-4 overflow-hidden">
      {Array.from({ length: 5 }).map((_, i) => (
        <div key={i} className="flex-shrink-0 w-72">
          <Skeleton className="h-8 w-24 mb-3" />
          <div className="space-y-3">
            <Skeleton className="h-24 w-full rounded-lg" />
            <Skeleton className="h-24 w-full rounded-lg" />
          </div>
        </div>
      ))}
    </div>
  );
}

// 리스트 스켈레톤
function ListSkeleton({ rows = 5 }: { rows?: number }) {
  return (
    <div className="space-y-2">
      {Array.from({ length: rows }).map((_, i) => (
        <Skeleton key={i} className="h-16 w-full rounded-lg" />
      ))}
    </div>
  );
}

// 그리드 스켈레톤
function GridSkeleton({ cards = 8 }: { cards?: number }) {
  return (
    <div className="card-grid">
      {Array.from({ length: cards }).map((_, i) => (
        <Skeleton key={i} className="h-32 w-full rounded-lg" />
      ))}
    </div>
  );
}

// 통계 카드 스켈레톤
function StatCardSkeleton() {
  return (
    <div className="rounded-lg border border-border-default p-4">
      <Skeleton className="h-4 w-20 mb-2" />
      <Skeleton className="h-8 w-32" />
    </div>
  );
}
```

**Suspense 통합:**
```typescript
// app/(app)/dashboard/page.tsx
export default function DashboardPage() {
  return (
    <>
      <div className="dashboard-grid">
        <Suspense fallback={<StatCardSkeleton />}>
          <ActiveOrdersStat />
        </Suspense>
        <Suspense fallback={<StatCardSkeleton />}>
          <MonthlyRevenueStat />
        </Suspense>
        {/* ... */}
      </div>
    </>
  );
}
```

### 8.2 에러 상태

```typescript
// components/ui/error-state.tsx
interface ErrorStateProps {
  title?: string;
  description?: string;
  retry?: () => void;
}

export function ErrorState({
  title = '데이터를 불러올 수 없습니다',
  description = '잠시 후 다시 시도해 주세요.',
  retry,
}: ErrorStateProps) {
  return (
    <div className="flex flex-col items-center justify-center py-12 text-center">
      <AlertTriangle className="h-12 w-12 text-danger mb-4" />
      <h3 className="text-heading-sm mb-2">{title}</h3>
      <p className="text-body-sm text-fg-secondary mb-4">{description}</p>
      {retry && (
        <Button variant="outline" onClick={retry}>
          다시 시도
        </Button>
      )}
    </div>
  );
}

// Error Boundary (app 수준)
// app/(app)/error.tsx
'use client';
export default function Error({ error, reset }: { error: Error; reset: () => void }) {
  return <ErrorState title="문제가 발생했습니다" description={error.message} retry={reset} />;
}
```

### 8.3 빈 상태

화면별 맞춤 빈 상태 메시지.

```typescript
const EMPTY_STATES: Record<string, EmptyStateProps> = {
  orders: {
    icon: ClipboardList,
    title: '등록된 수주가 없습니다',
    description: '첫 수주를 등록해 보세요.',
    action: { label: '수주 등록', href: '/orders/new' },
  },
  inventory: {
    icon: Package,
    title: '등록된 자재가 없습니다',
    description: '자재를 등록하면 재고 관리를 시작할 수 있습니다.',
    action: { label: '자재 등록', href: '/inventory/new' },
  },
  customers: {
    icon: Users,
    title: '등록된 고객이 없습니다',
    description: '수주 등록 시 고객이 자동으로 추가됩니다.',
    action: { label: '고객 등록', href: '/customers/new' },
  },
  schedule: {
    icon: CalendarDays,
    title: '예정된 일정이 없습니다',
    description: '수주를 등록하면 일정이 자동으로 생성됩니다.',
  },
  purchases: {
    icon: Truck,
    title: '등록된 발주가 없습니다',
    description: '재고 부족 시 발주를 등록할 수 있습니다.',
    action: { label: '발주 등록', href: '/purchases/new' },
  },
  finance: {
    icon: Wallet,
    title: '정산 내역이 없습니다',
    description: '수주/발주 완료 후 매출/매입이 기록됩니다.',
  },
  search: {
    icon: SearchX,
    title: '검색 결과가 없습니다',
    description: '다른 검색어로 시도해 보세요.',
  },
};
```

### 8.4 오프라인 상태

```typescript
// components/layout/offline-banner.tsx
'use client';

export function OfflineBanner() {
  const [isOnline, setIsOnline] = useState(true);

  useEffect(() => {
    const handleOnline = () => setIsOnline(true);
    const handleOffline = () => setIsOnline(false);
    window.addEventListener('online', handleOnline);
    window.addEventListener('offline', handleOffline);
    setIsOnline(navigator.onLine);
    return () => {
      window.removeEventListener('online', handleOnline);
      window.removeEventListener('offline', handleOffline);
    };
  }, []);

  if (isOnline) return null;

  return (
    <div className="fixed top-0 left-0 right-0 z-50 bg-warning px-4 py-2 text-center text-sm text-warning-contrast">
      오프라인 모드 - 캐시된 데이터만 표시됩니다
    </div>
  );
}
```

---

## 9. 파일 구조 (컴포넌트 중심 보완)

기존 계획서의 `src/components/` 구조를 상세화:

```
src/components/
├── ui/                          # Atoms: shadcn/ui (커스터마이징 가능)
│   ├── button.tsx               #   variant 추가 가능
│   ├── input.tsx
│   ├── card.tsx
│   ├── dialog.tsx
│   ├── sheet.tsx
│   ├── dropdown-menu.tsx
│   ├── tabs.tsx
│   ├── select.tsx
│   ├── badge.tsx                #   StatusDot 내장 가능 (L4)
│   ├── skeleton.tsx
│   ├── tooltip.tsx
│   ├── checkbox.tsx
│   ├── switch.tsx
│   ├── progress.tsx
│   ├── separator.tsx
│   ├── popover.tsx
│   ├── command.tsx
│   ├── form.tsx                 #   react-hook-form 래퍼
│   └── ...
│
├── common/                      # Molecules: shadcn Atoms 조합
│   ├── status-badge.tsx
│   ├── number-spinner.tsx
│   ├── price-display.tsx
│   ├── date-display.tsx
│   ├── customer-brief.tsx
│   ├── inventory-gauge.tsx
│   ├── empty-state.tsx
│   ├── error-state.tsx
│   ├── confirm-dialog.tsx
│   ├── search-input.tsx
│   ├── alert-banner.tsx
│   └── quick-add-sheet.tsx
│
├── layout/                      # 레이아웃 Organisms
│   ├── app-shell.tsx            # Sidebar + Header + Content
│   ├── sidebar.tsx
│   ├── header.tsx
│   ├── bottom-tab-bar.tsx
│   ├── page-header.tsx          # 타이틀 + 액션 + ViewSwitcher
│   └── offline-banner.tsx
│
├── providers/
│   ├── theme-provider.tsx
│   ├── query-provider.tsx       # React Query
│   └── supabase-provider.tsx
│
├── view-switcher/
│   ├── view-switcher.tsx
│   ├── responsive-view-switcher.tsx
│   └── view-container.tsx
│
├── orders/                      # 수주 도메인
│   ├── order-kanban-view.tsx
│   ├── order-list-view.tsx
│   ├── order-timeline-view.tsx
│   ├── order-card.tsx
│   ├── order-list-item.tsx
│   ├── order-form.tsx
│   ├── order-detail-sections.tsx
│   ├── order-timeline.tsx       # 변경 이력
│   ├── status-transition-button.tsx
│   ├── status-progress-indicator.tsx
│   ├── order-cancel-dialog.tsx
│   ├── order-materials-table.tsx
│   ├── shortage-alert-dialog.tsx
│   └── new-order-bottom-sheet.tsx
│
├── customers/
│   ├── customer-list.tsx
│   ├── customer-form.tsx
│   └── customer-quick-add.tsx
│
├── inventory/
│   ├── inventory-grid-view.tsx
│   ├── inventory-list-view.tsx
│   ├── inventory-card.tsx
│   ├── inventory-shortage-banner.tsx
│   └── inventory-adjust-form.tsx
│
├── purchases/
│   ├── purchase-list-view.tsx
│   ├── purchase-timeline-view.tsx
│   ├── purchase-form.tsx
│   └── receive-form.tsx
│
├── schedule/
│   ├── calendar-view.tsx         # [M12] @schedule-x 래퍼
│   ├── week-timeline-view.tsx
│   ├── agenda-view.tsx
│   ├── map-view.tsx              # [M11] Kakao Maps API 사용
│   ├── schedule-card.tsx
│   ├── schedule-form.tsx
│   └── schedule-conflict-warning.tsx
│
> **[M11] 지도 API 결정**: Kakao Maps API 사용. 국내 주소 기반 서비스이므로 한국 지도/주소 검색에 최적화된 Kakao 선택. `react-kakao-maps-sdk` 래퍼 사용. API 키는 환경변수 `NEXT_PUBLIC_KAKAO_MAP_KEY`로 관리.

> **[M12] @schedule-x 한국어**: 공식 한국어 로케일 지원 미확인. Sprint 1 초기에 PoC 진행 필수. 미지원 시 커스텀 로케일 파일 작성 (요일명, 월명, 시간 포맷 등).

├── finance/
│   ├── finance-summary-view.tsx
│   ├── finance-list-view.tsx
│   ├── finance-calendar-view.tsx
│   ├── stat-card.tsx
│   └── revenue-chart.tsx
│
├── dashboard/
│   ├── today-schedule-card.tsx
│   ├── order-pipeline-summary.tsx
│   ├── inventory-alert-list.tsx
│   ├── monthly-revenue-summary.tsx
│   └── quick-action-bar.tsx
│
├── modeling/
│   ├── closet-editor.tsx        # dynamic import (ssr: false)
│   ├── camera-mode-toggle.tsx
│   ├── component-palette.tsx
│   ├── dimension-overlay.tsx
│   ├── snap-grid.tsx
│   ├── editor-toolbar.tsx
│   └── material-selector.tsx
│
└── charts/
    ├── chart-wrapper.tsx        # 다크모드 어댑터
    ├── order-status-chart.tsx
    └── revenue-chart.tsx
```

---

## 10. 참조 문서 색인

| 항목 | 참조 위치 |
|------|-----------|
| 색상 토큰 (Light/Dark) | `dark-mode-multiview-ux-design.md` 1.1절 |
| CSS Variables | `dark-mode-multiview-ux-design.md` 1.2절 |
| Tailwind Config | `dark-mode-multiview-ux-design.md` 1.2절 |
| ThemeProvider 구현 | `dark-mode-multiview-ux-design.md` 1.3절 |
| 차트/캘린더 다크모드 | `dark-mode-multiview-ux-design.md` 1.4절 |
| 뷰 시스템 (ViewSwitcher, useViewState) | `dark-mode-multiview-ux-design.md` 2.3-2.4절 |
| 스케줄 뷰 와이어프레임 | `dark-mode-multiview-ux-design.md` 2.2.2절 |
| 타이포그래피 스케일 | `dark-mode-multiview-ux-design.md` 3절 |
| 모션/애니메이션 | `dark-mode-multiview-ux-design.md` 4절 |
| DB 스키마 | `closet-biz-system-final.md` 3절 |
| API 구조 (Server Actions/RPC) | `closet-biz-system-final.md` 3절 |
| 비즈니스 로직 (상태 전이) | `closet-biz-system-final.md` 4.2절 |
| 디렉토리 구조 | `closet-biz-system-final.md` 6절 |
| 구현 태스크 분해 | `closet-biz-system-final.md` 7절 |

---

**End of Frontend Design Document**

- 컴포넌트 총 수: ~70개 (Atoms ~15, Molecules ~12, Organisms ~43)
- 페이지 와이어프레임: 10개 화면
- 상태 관리: Server Components + React Query + react-hook-form + URL State
- 접근성: WCAG AA, 키보드 네비게이션, ARIA 패턴
- 에러/로딩/빈 상태: 뷰 타입별 스켈레톤, 화면별 빈 상태 메시지
